{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "dev": {
            "env": ["FRONTEND_PORT", "BACKEND_PORT", "BACKEND_SOURCE", "BACKEND_URL", "NEXT_PUBLIC_GOOGLE_MAP_API_KEY"]
        },
        "deploy": {
            "env": ["FRONTEND_PORT", "BACKEND_URL"]
        }
    }
}
