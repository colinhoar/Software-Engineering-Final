import { SignupForm } from "../components/signup_form.tsx";

const Signup = () => {
    return (
        <main id="signup" className="bg-linear-to-t from-gray-100 to-blue-50 signup">
            <SignupForm />
        </main>
    );
};

export default Signup;